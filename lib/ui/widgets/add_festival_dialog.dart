import 'package:flutter/material.dart';

class AddFestivalDialog extends StatelessWidget {
  const AddFestivalDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}